import 'package:flutter/material.dart';

class BlueThemeColors {
  static const Color primary = Colors.blue;
  static const Color background = Colors.lightBlue;
  static const Color text = Colors.red;
  static const Color headingText = Colors.red;
  static const Color subheadingText = Colors.red;
  static const Color buttonText = Colors.white;

  static const Map<String, Color> blueColors = {
    'primary': primary,
    'background': background,
    'text': text,
    'headingText': headingText,
    'subheadingText': subheadingText,
    'buttonText': buttonText,
  };
}
