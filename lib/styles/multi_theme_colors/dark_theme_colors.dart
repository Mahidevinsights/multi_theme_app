import 'package:flutter/material.dart';

class DarkThemeColors {
  static const Color primary = Colors.blueAccent;
  static const Color background = Color.fromARGB(255, 36, 35, 35);
  static const Color text = Colors.white;
  static const Color headingText = Colors.white70;
  static const Color subheadingText = Colors.white60;
  static const Color buttonText = Colors.black;

  static const Map<String, Color> darkColors = {
    'primary': primary,
    'background': background,
    'text': text,
    'headingText': headingText,
    'subheadingText': subheadingText,
    'buttonText': buttonText,
  };
}
